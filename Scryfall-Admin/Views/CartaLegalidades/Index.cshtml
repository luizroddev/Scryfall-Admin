@model IEnumerable<Scryfall_Admin.Models.CartaLegalidades>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Standard)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Modern)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Legacy)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Pauper)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Duel)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Predh)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Standard)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Modern)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Legacy)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Pauper)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Duel)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Predh)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
